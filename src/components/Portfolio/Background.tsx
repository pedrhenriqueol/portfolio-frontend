import React from 'react';
import InteractiveParticleField from './InteractiveParticleField';

/**
 * Background / Canvas de Partículas do Portfólio
 * Encapsula o campo físico de partículas com controle de lifecycle por viewport.
 */
export const Background: React.FC = () => {
    return <InteractiveParticleField />;
};

export default Background;
